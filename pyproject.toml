[project]
name = "hacs-ducobox-connector"
version = "1.0.0"
description = "HACS integration for Ducobox Connectivity Board"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
